<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">

    <shortcut
        android:shortcutId="shortcut_newEntry"
        android:enabled="true"
        android:icon="@mipmap/qc_entry_plus"
        android:shortcutShortLabel="@string/app_newEntry"
        android:shortcutLongLabel="@string/app_newEntry"
        android:shortcutDisabledMessage="@string/app_newEntry">
        <intent
            android:action="shortcut_newEntry"
            android:targetPackage="de.baumann.timetracker"
            android:targetClass="de.baumann.timetracker.activities.Activity_EditEntry" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list is what the user sees when they
             launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

</shortcuts>
        

